# OpenAI API Key
OPENAI_API_KEY=your-api-key

# Langfuse Configuration (Optional - for prompt management & observability)
# Get your keys from Langfuse Settings → API Keys after running `docker compose up` in langfuse/ directory
LANGFUSE_PUBLIC_KEY=pk-lf-***
LANGFUSE_SECRET_KEY=sk-lf-***
LANGFUSE_BASE_URL=http://localhost:3001

# Langfuse Managed Agent Configuration
# These settings control the Langfuse-managed agent which dynamically loads prompts from Langfuse
LANGFUSE_AGENT_PROMPT_NAME=mastra.langfuse.sample.system
LANGFUSE_AGENT_PROMPT_LABEL=production

# Prompt Variables (Mustache format: {{tone}}, {{channel}})
# These values will be injected into the Langfuse prompt template
LANGFUSE_PROMPT_VAR_TONE=professional
LANGFUSE_PROMPT_VAR_CHANNEL=web
